import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Boiler Repair Glasgow | Emergency Gas Safe Engineers",
  description:
    "Same-day boiler repair in Glasgow. Gas Safe 930395. No heat, no hot water, leaks, low pressure and ignition faults.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
